---
interface Props {
  schema: Record<string, unknown>;
}
const { schema } = Astro.props;
const withContext = { '@context': 'https://schema.org', ...schema };
---

<script type="application/ld+json" set:html={JSON.stringify(withContext)} is:inline />
